# money-shark-app â€” Command Line Interface (CLI)

Agent-native CLI generated by the **cli-anything** skill.

## ðŸš€ How to Run

### From PowerShell / Terminal:
```powershell
# Doctor diagnostics
.\bin\cli.ps1 doctor

# Show status
.\bin\cli.ps1 status

# JSON mode (machine-readable)
.\bin\cli.ps1 doctor --json

# Run development server
.\bin\cli.ps1 dev
```

### From Node:
```bash
node bin/cli.cjs doctor
node bin/cli.cjs status --json
```

## ðŸ“‹ Available Commands
- `doctor`: Health & environment check.
- `status`: Overview of dependencies, scripts, and configuration.
- `dev`: Launch development server.
- `build`: Build production assets.
- `list-files`: List directory contents.